# 첫 배포 전에 실제 도메인으로 바꾼다.
moca.example.com {
    encode zstd gzip

    handle /api/* {
        reverse_proxy 127.0.0.1:8080
    }

    handle {
        root * /opt/moca/current/frontend/dist
        try_files {path} /index.html
        file_server
    }
}
