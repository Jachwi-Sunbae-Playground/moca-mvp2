spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:jachwi_sunbae}?allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USERNAME:jachwi_sunbae}
    password: ${DB_PASSWORD:local_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
